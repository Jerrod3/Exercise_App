{
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "backend": "^0.0.0"
  },
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start": "npm start --prefix backend",
      "install-client": "cd Lepperj_react-2 && npm install && npm run build && cd ..",
      "install-server": "cd backend && npm install && cd .. ",
      "heroku-postbuild": "npm run install-server && npm run install-client"
  }
}
