{
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "backend": "^0.0.0"
  },
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start": "node controller_exercises.mjs",
      "heroku-postbuild": "cd Lepperj_react-2 && npm install && npm run build"
  }
}
