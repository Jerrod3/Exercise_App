{
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "backend": "^0.0.0"
  },
    "scripts": {
      "start": "node backend",
      "heroku-postbuild": "cd ./Lepperj_react-2 && npm install && npm run build"
      
  }
}
